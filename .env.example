# Local dev: copy to `.env` in this folder (`cp .env.example .env` or Copy-Item on Windows).
# Optional overrides (see platform/index.ts for defaults):
# PLATFORM_PORT=3850
# PLATFORM_DB_PATH=
# PLATFORM_UPDATES_DIR=
# AMAAN_ROLLING_SYNC_MAX_MS=
